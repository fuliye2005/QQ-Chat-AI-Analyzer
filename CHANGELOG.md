# 更新日志

## 2026-09-03：修复多年分析与并发失败语义

- 统一使用 `Asia/Shanghai` 进行消息时间归属，修复带偏移 ISO 时间跨年、数值型 `time` 和无效 `time` 回退 `timestamp` 的问题。
- 显式规范化 `recalled` / `isRecalled`，避免字符串 `"false"` 被当作真值。
- 按每个年份的实际覆盖月份选择完整年度或阶段性 Prompt，集合报告保留各年份边界。
- 增加 Map/Reduce JSON 结构校验；单个 Map 请求失败时继续收集其他成功结果，并在中间产物中保存失败和待处理清单。
- `per_year`、`combined`、`both` 支持部分成功状态，已生成的报告链接不会因其他报告失败而丢失。
- 修复默认 Mock 的 Map/Reduce 返回契约，并为 `max_completion_tokens` / `max_tokens` 增加仅针对参数不兼容错误的回退。
- 新增离线 pytest 测试，覆盖时间、年度隔离、部分失败、报告模式、Mock、Schema 和 token 参数兼容。
- 中间产物当前仅用于诊断和后续恢复基础，尚未提供进程重启恢复或独立重试入口。

## 2026-09-02：多年记录分析与稳定性优化

这份说明总结了本次开发版本相对于原主分支的主要修复和优化。相关代码目前已经合并到 `main` 分支。

### 主要修复

- **修复时间识别问题**：同时支持导出文件中的 ISO 时间和秒、毫秒、微秒、纳秒时间戳，不再把毫秒时间错误地解析到 1970 年附近。
- **修复撤回消息识别**：同时兼容 `isRecalled` 和 `recalled` 两种字段。
- **改进异常提示**：AI 请求超时、连接失败、返回内容不是合法 JSON 时，会记录具体原因并正确标记失败，不再把失败结果伪装成“分析完成”。
- **增加请求诊断信息**：日志中会记录请求阶段、模型、输入大小、超时时间、输出上限、重试次数和失败原因，排查问题更方便。
- **修复报告显示效果**：调整报告 CSS 的插入位置，改善卡片、文字和背景的对比度，减少文字看不清的问题。
- **本地配置与历史记录分离**：用户自己的配置和历史记录保存在本地文件中，仓库只保留不含密钥的默认配置模板。

### 多年份分析优化

- 导入多年聊天记录后，可以先选择需要分析的年份。
- `每个年份分别生成` 模式会为每个年份单独统计、单独汇总，并生成独立的 HTML 报告，不会把几年内容混成一份。
- 保留了多年集合报告通道，也可以选择同时生成年度报告和集合报告。
- Map 阶段支持并发请求，并增加并发数设置；程序会对并发数和输入预算做范围限制，降低服务掉线的概率。
- 当单次输入过大时，会在消息边界处分段发送，避免一次请求超过模型服务的实际处理能力。
- Map 阶段会保存中间产物。即使后面的汇总或报告生成失败，也能保留已经完成的分片结果，便于诊断失败原因并为后续恢复功能提供基础；当前版本尚未提供进程重启恢复、Map 重试或独立 Reduce 重试入口。
- 历史记录会保存报告年份和报告类型，便于区分年度报告和集合报告。

### 测试结果

- 已用包含多个年份的聊天记录进行测试。
- 多年记录导入后，按年份拆分并生成年度报告的流程已验证成功；在另一台设备上的完整测试中，`2023`、`2024`、`2025`、`2026` 四个年份的报告都成功生成。
- Map 分析阶段共完成 `14/14` 个分片请求，说明多年数据拆分和分片分析可以正常工作。

### HTML 加强结果

- 本次测试开启了“HTML 加强”功能。
- 报告主体已经正常生成，但后续的 HTML/CSS 加强请求没有成功：第一次请求遇到服务端 `524` 超时，重试时又发生连接失败。
- 因此最终保留了未加强的原始 HTML 报告；这次失败不影响报告主体内容的生成。

### 相关提交

- `9b4e6b3`：修复解析、LLM 诊断和报告渲染问题。
- `f68da98`：加入多年分析和 Map 并发处理。
- `fb42fe0`：按年份拆分报告并保存 Map 中间产物。
