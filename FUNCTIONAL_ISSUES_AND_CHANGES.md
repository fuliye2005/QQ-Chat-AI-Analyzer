# 功能问题与修改记录

## 审查范围

- 审查日期：2026-08-19
- 最新仓库提交：2026-01-13
- 本记录只关注功能、数据正确性和运行体验，不讨论安全问题。
- 测试文件：`group_🐰🐶_齋_162549240_20260819_115418609.json`

## 已确认的问题

### 1. 时间字段解析错误

测试文件中的消息同时包含：

- `time`：ISO 格式时间，例如 `2026-05-19T04:04:24Z`
- `timestamp`：13 位毫秒时间戳，例如 `1779163464000`

旧代码只读取 `timestamp`，并调用 `pd.to_datetime(timestamp)`，导致毫秒时间戳被按纳秒解析，消息时间落到 1970 年附近。旧代码也没有读取 `time` 字段。

### 2. 撤回字段不兼容

测试文件使用 `recalled`，旧代码只识别 `isRecalled`，因此撤回消息统计会不准确。

### 3. 默认 Mock 模式无法生成正常报告

Mock 客户端返回 HTML 片段，但 Map/Reduce 生成器按 JSON 解析，导致解析失败。任务仍可能显示为“完成”，报告内容则是失败占位文本。

### 4. AI 错误可能被伪装成完成

模型超时、返回非 JSON 或返回不完整字段时，生成器会吞掉异常并返回 fallback 内容，任务状态仍然可能是 `completed`。

### 5. 跨年数据切分不完整

季度切分的第四季度只有开始时间，没有限制到下一年 1 月；跨年数据可能被丢弃或被错误归入上一年度第四季度。

### 6. Token 预算没有真正限制 API 请求

`max_tokens` 只参与本地字符估算和采样，没有传入实际 LLM 请求。当前采样也没有使用项目中另外实现的完整自适应策略。

### 7. 媒体和撤回消息在 AI 采样中信息不足

主流程的 `smart_sample` 只读取 `content`，没有根据消息类型补充图片、视频和撤回标记，纯媒体消息进入 AI 时可能变成空行。

### 8. 统计总数可能与实际解析数量不一致

主流程使用 JSON 元数据中的 `statistics.totalMessages` 覆盖实际解析出的 DataFrame 行数。若文件元数据和有效消息数不一致，报告数字会错误。

### 9. AI 汇总上下文缺少部分统计数据

活跃用户数、潜水用户数、复读机榜、最长连续发言等字段没有完整计算或传入，Reduce Prompt 中可能出现 `0`、空列表或空对象。

### 10. 用户排行榜的身份分组不一致

部分统计按昵称分组，部分统计按 `user_id + user_name` 分组。用户改名或多人同名时，排行榜可能拆分或合并错误。

### 11. 部分文档宣传功能没有接入报告

小时活跃度、每日活动数据和词云分析代码存在，但当前报告模板没有完整展示这些结果。

### 12. Refine 功能可能覆盖正常报告

最终增强只检查返回内容长度，没有验证返回值是否为完整 HTML。模型错误信息或 HTML 片段可能覆盖原始报告。

### 13. 历史记录群名可能显示默认值

历史记录使用了 `stats['title']`，解析器实际保存的字段是 `stats['chat_name']`，因此历史列表可能显示“QQ聊天记录”而不是实际群名。

## 已完成的修改

### 时间和撤回字段兼容

修改文件：

- `src/parser.py`
- `src/registry.py`

具体修改：

1. 优先解析 `time` ISO 时间字段。
2. 没有 `time` 时，兼容秒、毫秒、微秒和纳秒时间戳。
3. 统一使用带 UTC 时区的时间，避免时区感知和无时区时间混用。
4. 同时识别 `isRecalled` 和 `recalled`。
5. 无法解析时间时跳过该条消息，不再用当前时间伪造聊天时间。
6. 保留旧版 ISO 字符串时间戳的兼容性。

## 修复验证

使用原始测试文件验证：

- 解析消息数：`39871`
- 解析时间范围：`2026-05-19` 至 `2026-08-19`
- 目标年份：`2026`
- 撤回消息数：`215`
- 时间类型：带 UTC 时区的 Pandas datetime

使用裁剪后的测试文件验证：

- 文件：`C:\Users\Administrator\Desktop\group_🐰🐶_齋_162549240_20260819_115418609_from_20260801.json`
- 消息数：`20599`
- 时间范围：`2026-08-01 00:28:41 UTC` 至 `2026-08-19 03:52:12 UTC`
- 撤回消息数：`109`
- 目标年份：`2026`

## 2026-08-19：长请求超时和日志可见性修复

修改文件：

- `app.py`
- `src/llm_client.py`
- `src/generator.py`
- `src/registry.py`
- `config.json`

具体修改：

1. 真实 LLM 请求超时从原来的 60 秒提高为可配置值，当前配置为 180 秒。
2. 增加输出 Token 上限，当前为 8000，避免 API 使用不受控的默认输出预算。
3. 自定义模式显式传入 `mode=custom`。
4. 每次请求记录阶段、模型、输入字符数、粗略输入 Token 数、输出上限、超时时间、重试次数和耗时。
5. 记录每次重试的错误类型和错误文本。
6. 真实 API 请求失败后不再静默退回 Mock，避免把真实错误伪装成演示结果。
7. Map 或 Reduce 的 JSON 解析失败会记录到任务日志，并使任务进入失败状态，而不是生成一份所有栏目都显示“生成失败”的假完成报告。

当前 LLM 请求配置：

- `request_timeout_seconds`: `180`
- `request_max_output_tokens`: `8000`
- `request_max_retries`: `2`

## 2026-08-19：报告 CSS 和文字对比度修复

修改文件：

- `templates/report.html`
- `src/renderer.py`

具体修改：

1. 修复模板中重复 CSS 块被放在第一个 `</style>` 外的问题，避免 CSS 文本直接显示在报告顶部。
2. 报告卡片、统计块、关键词标签和排行榜改用主题 `card_bg`，避免深色文字主题与浅色卡片或浅色文字主题与白色卡片冲突。
3. 降低背景渐变和悬停效果对主题颜色的干扰。
4. 渲染前校验 AI 返回的颜色值，并在文字与卡片背景对比度过低时自动使用可读的深色或浅色文字。

## 尚未修改

除已经列出的修改外，下面这些项目目前只完成了定位和记录，尚未修改源码。后续建议优先处理：

1. 修复 Mock 输出契约，使演示模式也能返回 Map/Reduce 所需的 JSON。
2. 修复跨年切分与实际消息总数统计。
3. 重做 Token 预算和媒体消息采样。
4. 为 Refine 输出增加完整 HTML 校验。
5. 补齐统计数据和报告图表展示。
